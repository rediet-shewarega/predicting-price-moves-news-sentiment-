# Scripts

- `generate_sample_data.py` — builds `data/raw/fnspid_sample.csv` and `data/raw/stock_prices_sample.csv` for notebooks and CI when the full FNSPID export is not available.

Replace these with your real FNSPID and YFinance extracts; keep the same column names expected by the notebooks.
